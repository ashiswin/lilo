<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreferenceCompat
        android:key="prefSolver"
        android:title="Fast Route Solver"
        android:summaryOn="Using fast route planner"
        android:summaryOff="Using slow route planner" />

    <SwitchPreferenceCompat
        android:key="prefOffline"
        android:title="Offline Mode"
        android:summary="Disable photo stream loading for no-data setups" />

</PreferenceScreen>